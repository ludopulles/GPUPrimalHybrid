This branch `eprint-2026-366` is a copy from [a fork](https://github.com/identitymapping/enhanced_hybrid_attack/) of this repository.
This fork was used in the [paper](https://eprint.iacr.org/2026/366) to further improve the Guess & Verify attack with exploitation of the module structure.

---
# Enhanced Hybrid Decoding Attack (modified version)

[This code](https://github.com/identitymapping/enhanced_hybrid_attack/) is a modification of the original [GPUPrimalHybrid](https://github.com/ludopulles/GPUPrimalHybrid), commit 20f548bd.

## Main Modifications

### 1. `enhanced_attack.py`: Enhanced Hybrid Decoding Attack Using Algebraic Structure

This file implements the **enhanced hybrid decoding attack using algebraic structure**, modified from `attack.py`. Key changes:

- Modified `solve_guess` function:
  Builds the `b_vecs` array containing each `vec(x^{rotind} · b)`, corresponding to new LWE instances with secret `vec(x^{rotind} · s)`.

- Added `solve_guess_fp64_nr_projected_multi_target` function:
  For every `b_vec` in `b_vecs` (i.e., `vec(x^{rotind} · b)`), solve using the GPU implementation of Babai’s NP algorithm.
  This is a multi-target version of `svp_babai_fp64_nr_projected` (which takes only one `b_vec`). If any `vec(x^{rotind} · s)` is recovered, the attack succeeds.  **This function is currently only a validation implementation and can be further optimized**.

Other functions remain mostly unchanged.

### 2. Modifications to `lattice-estimator`

Mainly modified the `cost` function in the `PrimalHybrid` class in `lwe_primal.py`:

- Use algebraic structure: introduce a variable `bound` equal to the degree of the polynomial ring.
- Time cost: multiply the cost of `T_{bdd}` (here Babai’s NP algorithm) by `bound` (in code: `svp_cost.repeat(ssf(search_space)*bound)`).
- Success probability for the enhanced hybrid attack: updated to `RR(1) - (RR(1) - probability)**bound`, computed by the `new_prob` function.

### 3. Improved Search for Optimal Dropped Dimension `k`

The original `lattice-estimator` uses binary search + gradient descent (`local_minimum`) to find the optimal dropped dimension `k` (`zeta` in the `__call__` function of the `PrimalHybrid` class), which may get stuck in local optima for the enhanced hybrid decoding attack.

We replaced it with an **exhaustive two-stage search**:
1. Coarse-grained search with large step sizes
2. Fine-grained search with small step sizes
to find the globally optimal \(k\) (see the `__call__` function of `PrimalHybrid`).

### 4. The number of iterations from Estimator

We now set the number of iterations required to reach the target success probability using the value estimated by `lattice-estimator`, and output it (see `estimation.py`).

## How to Run

### Run the Enhanced Hybrid Decoding Attack

1. Modify `enhanced_attack_params.py`:
   **Ensure that the parameter `'structure_leverage': True` is added**, which enables using algebraic structure in the bit-security evaluation by `lattice-estimator`.

2. Run:
   ```bash
   python enhanced_attack.py
   ```

### Run the Original Pulles et al. Code

1. Modify `attack_params.py`
2. Run:
   ```bash
   python attack.py
   ```

### Bit-Security Estimation Only (No Actual Attack)

We also provide `attack_estimate.py`, which outputs the bit security of parameters without performing the actual attack:

```bash
python attack_estimate.py
```

## Experimental Results
- `results/attack_results`: Records the intermediate runtime results of the program when `'structure_leverage': False` is set, corresponding to Table 4.2 in the paper.
- `results/enhanced_attack_results`: Records the intermediate runtime results of the program when `'structure_leverage': True` is set, corresponding to Table 4.3 in the paper.
- `results/newrecords`: Records the experimental results of new records obtained by using the enhanced guess-and-verify decoding attack, corresponding to Table 4.4 in the paper.
- `results/attack_estimate_results.txt`: Contains the estimation results of bit security for some parameters, generated by running the `attack_estimate.py` file.

---
# Guess + Verify implementation (original README)

This repository contains an implementation of the Guess + Verify attack against the Learning with Errors (LWE) with sparse secrets (binomial or ternary), using GPUs.

## Requirements and dependencies

Hardware:

- CUDA-compatible GPU(s), or cupy with ROCm for AMD GPUs (untested)

Software:

- CUDA toolkit, recommended: version >=11.0
- `conda`, `mamba`, or a similar environment manager (recommended is `conda`, see `install.sh`)
- `cuBLASter`: BLASter implementation adapted for GPUs
- `G6K-GPU-Tensor`: GPU-accelerated lattice sieving
- `lattice-estimator`: used to find good attack parameters based on the estimated cost of the attack

## Installation

1. Install `conda` if you haven't already, see [conda docs](https://www.anaconda.com/docs/getting-started/miniconda/install).
2. Run the installation scripts: `./install.sh`
3. Install all required Python packages *in the environment*: `pip install -r requirements.txt`

## Main Components

``attack_testing.py``: Contains the main logic for the primal attack, including LWE instance creation, lattice embedding, reduction, SVP, and Babai's algorithm. It contains all functions for single-threaded execution (used for testing parameters, avoid using it for benchmarking).

``attack_for_benchmark.py``: Implements multi-GPU parallelization, distributing the workload across available GPUs and managing worker processes. Use this for actual attacks. (see Usage section below for details)
This code is also cleaner and easier to read than attack_testing.py because it does not contain all the testing code.

``attack_params.py``: Contains a list of parameter sets for different LWE instances to be attacked. You can modify this file to add or change the parameters for your specific use cases.

## Parameters

Set the numbers of workers and chunk size in `attack.py` according to your hardware capabilities.

- `num_workers`: Number of parallel workers (ideally a multiple of the number of GPUs and CPU cores, but may be limited by GPUs VRAM)
- `GUESS_BATCH`: Number of batches to process in each guess (affects GPUs memory usage and performance)

### Lattice Embedding

The system creates LWE instances in the file `lwe.py`.

- The binomial setting is generated using `generate_CBD_MLWE`
- The ternary setting is generated using `generate_ternary_MLWE`

The embedding and ternary LWE instance creation are taken from the [Cool+Cruel=Dual](
    https://gitlab.com/fvirdia/cool-plus-cruel-equals-dual
) repository. We also more generally adopted their file naming conventions, as this code is based on theirs. Many thanks to the authors.

### Result Handling

The results of the attack are collected and saved in a CSV file, including:

- `run_id`: Unique identifier for the run
- `n`: Dimension of the LWE instance
- `q`: Modulus
- `w`: Hamming weight of the secret
- `secret_type`: Type of secret distribution (ternary or binomial)
- `sigma`: Standard deviation for Gaussian errors (if applicable)
- `eta`: Parameter for binomial secrets (if applicable)
- `success`: Whether the attack was successful
- `iterations_used`: Number of performed iterations before the solution is found
- `time_elapsed`: Wall time taken for the attack
- `error`: Any error encountered during the attack (if applicable)

## Usage

To run the attack, execute the following command in your environment:

```bash
python attack.py
```

To see all command line arguments, execute `python attack.py -h` in your environment.
In particular, you can specify the number of workers (X) with `-wX`.

In `attack_params.py` you can add an instance by specifying the following parameters in a dictionary:

- `n`: Dimension
- `q`: Modulus
- `w`: Hamming weight of secret

In addition you need to specify `type_binomial(eta, k_dim)` for the binomial setting and `type_ternary(lwe_sigma)` for the ternary setting.
You can skip attack parameter selection, by adding the following parameters:

- `beta`: BKZ block size,
- `eta_svp`: only value 2 is supported currently, which corresponds to Babai NP as CVP-solver,
- `m`: number of LWE samples to pick from the structured MLWE(/RLWE) instance,
- `k`: number of positions in the secret to guess,
- `h_`: weight of the guessed part of the secret.
